import {
  NEWSFEED_GET_ITEMS,
  NEWSFEED_NEXT_ITEM
} from '../mutation-types';

const state = {
  newsfeedItems: [],
  currentItem: 0
};
const mutations = {
  [NEWSFEED_GET_ITEMS] (state) {

  },
  [NEWSFEED_NEXT_ITEM] (state) {

  }
};
const getters = {};
const actions = {};

export default {
  state,
  mutations,
  getters,
  actions
};
